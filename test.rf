k := (1 + 2) * 3;

add_ten := func(v : int) -> int {
    return 10 + v;
}

variadic := func(j : cstring, b : ...) -> int {
    return 10;
}

main := func() -> int {
    l := printf("yes", 6);
    return 0;
}
