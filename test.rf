import "standard/string.rf";
import "standard/io.rf";

struct just_a_struct {
    int alpha;
    int beta;
}

int main() {
    //just_a_struct s = (just_a_struct) { .alpha = 10, .beta = 20 };
    //just_a_struct t = (just_a_struct) { .alpha = 1, .beta = 2 };
    //s += t;
    
    std.string foo = std.get_str("this is a test");

    printf("%s\n", std.get_cstr(foo));
    
    return 0;
}
