native void printf(string format, ... rest);

struct just_a_struct {
    int alpha;
    int beta;
}

int main() {
    just_a_struct s = (just_a_struct) { .alpha = 10, .beta = 20 };

    int i = 0;
    
    i += 1;
    i -= 4;
    i *= 2;
    i /= 6;

    printf("%d\n", i);
    
    return 0;
}
