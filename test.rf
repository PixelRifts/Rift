k := (1 + 2) * 3;

add_ten := func(v : int) -> int {
    return 10 + v;
}

variadic := func(j : int, b : ...) -> int {
    return j;
}

main := func() -> int {
    t := variadic(10, 10, 20);
    return 0;
}
