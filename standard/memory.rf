import "int.rf";

native void* malloc(long size);
native void* realloc(void* block, size_t size);
native void* calloc(int count, int elem_size);
native void* memset(void* block, int ch, size_t size);
native void* memcpy(void* to, void* from, size_t size);
native void* memmove(void* to, void* from, size_t size);
native void* memcmp(void* to, void* src, size_t lenbytes);
native void* memchr(void* block, int ch, size_t size);
native void  free(void* buffer);