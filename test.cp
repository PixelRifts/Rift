void native printf(string format, ... rest);
void* native calloc(int count, int elem_size);
void native free(void* buffer);

void fooxyz(int x) {
	
}

int main() {
	void*(int)* (int)* function = fooxyz;
	function(10);
	return 0;
}
